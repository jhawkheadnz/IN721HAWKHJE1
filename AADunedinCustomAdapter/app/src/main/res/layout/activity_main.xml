<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="bit.hawkhje1.allaboutdunedindrawer.MainActivity"
    android:orientation="vertical">
    <LinearLayout
        android:id="@+id/content_frame"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/imageView"
            android:src="@drawable/octagon"
            android:adjustViewBounds="true"
            android:baselineAlignBottom="false"
            android:contentDescription="@string/app_header_description" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/main_title"
            android:id="@+id/txtTitle"
            android:textSize="@dimen/title_font_size"
            android:gravity="center_vertical|center_horizontal"
            android:height="@dimen/title_height"
            android:background="@color/colorMainPageTitle"
            android:textColor="@android:color/white" />
    </LinearLayout>

    <ListView
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:id="@+id/left_drawer"
        android:background="@android:color/white"
        android:choiceMode="singleChoice"
        style="@style/Theme.AppCompat.Light.NoActionBar" />
</android.support.v4.widget.DrawerLayout>
